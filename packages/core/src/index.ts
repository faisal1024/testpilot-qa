/**
 * @testpilot/core — shared types and utilities.
 *
 * Milestone 1 foundation only. Real shared types (Finding, LocatorContext, Rule,
 * Reporter, etc.) are introduced in their respective feature phases.
 */

export const VERSION = '0.0.0'
